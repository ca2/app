#pragma once


//string get_error_message(::u32 dwError);


//namespace windows
//{


//   class thread;


   //CLASS_DECL_AURA HINSTANCE     load_library(const char * psz);

   //CLASS_DECL_AURA bool          shell_get_special_folder_path(::windowing::window * pwindow,::file::path &str,i32 csidl,bool fCreate);
   //CLASS_DECL_AURA ::file::path  shell_get_special_folder_path(i32 csidl, bool fCreate = true, ::windowing::window * pwindow = nullptr);
   //CLASS_DECL_AURA ::u32         get_file_attributes(const char * pFileName);

   //CLASS_DECL_AURA ::u32         get_current_directory(string & str);
   //CLASS_DECL_AURA ::u32         get_temp_path(string & str);
   //CLASS_DECL_AURA ::i32         reg_query_value(HKEY hkey,const char * pszSubKey,string & str);

   //CLASS_DECL_AURA hicon         extract_icon(HINSTANCE hInst,const char * pszExeFileName,::u32 nIconIndex);

   //CLASS_DECL_AURA bool          delete_file(const char * pFileName);

   //CLASS_DECL_AURA i32           get_menu_string(HMENU hMenu,::u32 uDItem,string & str,::u32 flags);
   //CLASS_DECL_AURA void          time_to_filetime(::object * pobject,const ::datetime::time& time,LPFILETIME pFileTime);


//} // namespace windows



//CLASS_DECL_AURA ::i32 delete_registry_tree_helper(HKEY hParentKey,const string & strKeyName);


//CLASS_DECL_AURA __pointer(::aura::application) __get_app();
//CLASS_DECL_AURA HINSTANCE __get_resource_handle();
//CLASS_DECL_AURA void __set_resource_handle(HINSTANCE hInstResource);

//CLASS_DECL_AURA HINSTANCE __get_resource_handle();
//CLASS_DECL_AURA HINSTANCE __find_string_resource_handle(::u32 nID);


//CLASS_DECL_AURA i32 app_main(::aura::system * psystem, HINSTANCE hInstance, HINSTANCE hPrevInstance, char * pCmdLine, ::e_display edisplay);



