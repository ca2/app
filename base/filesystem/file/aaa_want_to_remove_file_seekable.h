#pragma once


namespace file
{


   //class CLASS_DECL_AURA stream_get_size :
   //   virtual public ::object
   //{
   //public:
   //   virtual i32 GetSize(u64 * psize) const = 0;
   //};

   //enum e_seek
   //{
   //   seek_begin     = 0x0,
   //   seek_current   = 0x1,
   //   seek_end       = 0x2,
   //   beg = seek_begin,
   //   cur = seek_current,
   //   end=seek_end
   //};


/*   class CLASS_DECL_AURA seekable :
      virtual public ::object
   {
   public:

      virtual void seek_to_begin();
      virtual filesize seek_to_end();


      virtual filesize seek_from_begin(filesize position);
      virtual filesize seek(filesize offset, e_seek seekOrigin);
      virtual filesize get_position() const;

   };


      --- */


} // namespace file


