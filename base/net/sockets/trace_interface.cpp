#include "framework.h" 
#include "aura/net/sockets/_.h"


namespace sockets
{


   //void trace_logger::sockets_log(base_socket_handler * phandler, base_socket * sock, const string & strUser, i32 iError, const string & strSystem, e_trace_level elevel)
   //{

   //   string strLevel = trace_level_name(elevel);

   //   if (sock)
   //   {
   //      Sys(phandler->get_context_application()).log().trace("fd %d :: %s: %d %s (%s)\n", sock->GetSocket(), strUser.c_str(), iError, strSystem.c_str(), strLevel.c_str());
   //   }
   //   else
   //   {
   //      Sys(phandler->get_context_application()).log().trace("%s: %d %s (%s)\n", strUser.c_str(), iError, strSystem.c_str(), strLevel.c_str());
   //   }

   //}


} // namespace sockets



